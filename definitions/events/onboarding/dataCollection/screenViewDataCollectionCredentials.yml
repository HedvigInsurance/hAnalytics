name: screen_view_data_collection_credentials
description: When data collection credentials screen is shown (Insurely)
accessor: screenViewDataCollectionCredentials
inputs:
  - name: provider_id
    type: String
    argument: providerId