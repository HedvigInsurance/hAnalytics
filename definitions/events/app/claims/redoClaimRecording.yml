name: redo_claim_recording
description: When a member clicks "record again" when recording a claim
accessor: redoClaimRecording
inputs:
  - name: claim_id
    type: String
    argument: claimId
  - name: claim_status
    type: String
    argument: claimStatus
targets:
  - Kotlin
  - Swift
