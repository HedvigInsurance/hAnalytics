name: stop_claim_recording
description: When a claim recording is stopped
accessor: stopClaimRecording
inputs:
  - name: claim_id
    type: String
    argument: claimId
  - name: claim_status
    type: String
    argument: claimStatus
targets:
  - Kotlin
  - Swift
