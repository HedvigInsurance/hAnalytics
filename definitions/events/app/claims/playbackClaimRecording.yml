name: playback_claim_recording
description: When a user playbacks their claims recording before submitting it
accessor: playbackClaimRecording
inputs:
  - name: claim_id
    type: String
    argument: claimId
  - name: claim_status
    type: String
    argument: claimStatus
targets:
  - Kotlin
  - Swift
