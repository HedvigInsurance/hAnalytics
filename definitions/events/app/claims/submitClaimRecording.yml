name: submit_claim_recording
description: When a member submits a claim recording
accessor: submitClaimRecording
inputs:
  - name: claim_id
    type: String
    argument: claimId
  - name: claim_status
    type: String
    argument: claimStatus
targets:
  - Kotlin
  - Swift
