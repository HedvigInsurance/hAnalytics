name: experiment_evaluated
description: Experiment where evaluated, typically means it was shown on screen or similar
accessor: experimentEvaluated
inputs:
  - name: name
    type: String
    argument: name
  - name: variant
    type: String
    argument: variant
targets:
  - Kotlin
  - Swift
  - JS
