name: SCREEN_VIEW_FOREVER
accessor: testGraphqlEvent
inputs:
  - name: NUMBER_OF_REFERRALS
    type: Integer
    argument: numberOfReferrals
constants:
  - name: HELLO
    type: Integer
    value: 0
graphql:
  query: |
    query AnalyticsMemberID {
      member {
        id
      }
    }
  getters:
    - name: MEMBER_ID
      getter: member.id